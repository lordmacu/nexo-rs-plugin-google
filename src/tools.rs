//! Phase 94 placeholder — descriptive `tool_def_*()` helpers land
//! in step 6. The four `ToolHandler` structs from the in-tree
//! crate are NOT ported: outbound dispatch is a single
//! `outbound_tool.invoke` JSON-RPC handler (see `plugin.rs`).
